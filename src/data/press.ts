import type { PressItem } from "@/types/content";

/**
 * Press mentions. No articles/interviews were supplied in the brief, so this
 * stays empty until real press material is confirmed. The Press section
 * renders an appropriate empty state. See README > "Onde colocar imprensa".
 */
export const press: PressItem[] = [];
