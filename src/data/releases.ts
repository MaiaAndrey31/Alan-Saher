import type { Release } from "@/types/content";

/**
 * Music releases. The brief confirms Alan Saher has original productions on
 * digital platforms, but no titles, artwork or streaming links were provided.
 * Do NOT invent discography or stream counts — keep this empty (the Music
 * section renders a "connect on Spotify/Apple Music" state instead) until
 * real release data is supplied. See README > "Como cadastrar releases".
 */
export const releases: Release[] = [];
