import type { Show } from "@/types/content";

/**
 * Upcoming shows. No agenda has been provided yet — keep this array empty
 * until real dates are confirmed. Do NOT invent events.
 *
 * To add a show:
 * shows.push({
 *   date: "2026-12-31",
 *   city: "Alfenas",
 *   venue: "Venue Name",
 *   country: "Brazil",
 *   ticketUrl: "https://...",
 * });
 */
export const shows: Show[] = [];
